# Самопроверка проекта (self-checklist)

Отметьте, что реализовано в вашем проекте.  
Этот чеклист помогает и вам, и преподавателю быстро понять, на каком уровне готовности находится проект.

В столбце «Да/Нет (студент)» поставьте `✅` или `❌`.  
В столбце «Где смотреть / комментарий» укажите файл(ы), где реализован соответствующий пункт.

> Важно: чеклист – это **самооценка**. Преподаватель может не согласиться с вашими отметками и скорректировать оценку.

---

## Таблица самопроверки

| #  | Критерий                                                                 | Да/Нет (студент) | Где смотреть / комментарий                          |
|----|---------------------------------------------------------------------------|------------------|-----------------------------------------------------|
| 1  | Сервис запускается по инструкциям из `project/README.md` и работает      | ✅ | `README.md` раздел «Как запустить». Команда: `uvicorn src.service.api:app --reload`. Проверено на `http://localhost:8000/docs` |
| 2  | Endpoint `/predict` использует **реальную модель**, а не заглушку        | ✅ | Эндпоинт `POST /ask` в `src/service/api.py`. Использует `EmbeddingSearch` (sentence-transformers) или `TFIDFSearch` (scikit-learn) — реальные модели |
| 3  | Есть EDA и хотя бы один эксперимент с метриками                          | ✅ | `notebooks/exp01_eda_baseline.ipynb` — EDA: статистика, графики, анализ чанков. Метрика Hit@3 в `notebooks/exp02_tfidf_search.ipynb` |
| 4  | Есть baseline и улучшенная модель, есть **сравнение по метрикам**        | ✅ | Baseline — TF-IDF (`src/models/tfidf_search.py`). Улучшенная — Embeddings (`src/models/embedding_search.py`). Сравнение Hit@3 в `notebooks/exp03_embeddings.ipynb` и `report.md` раздел 5 |
| 5  | Код не свален в один ноутбук: есть внятная структура в `src/`            | ✅ | `src/data/loader.py`, `src/models/tfidf_search.py`, `src/models/embedding_search.py`, `src/models/llm_generator.py`, `src/service/api.py` |
| 6  | Есть Dockerfile **или** понятный сценарий развёртывания без Docker       | ✅ | `Dockerfile` в корне проекта. Команды: `docker build -t rag-service .` и `docker run -p 8000:8000 rag-service`. Также работает без Docker через `uvicorn` |
| 7  | Есть `.env.example` и **нет** в репозитории реальных секретов/паролей    | ✅ | `configs/.env.example` содержит шаблон без реальных значений. `.env` в `.gitignore`. Реальных API-ключей в коде нет — основной функционал не требует ключей |
| 8  | Реализованы логи/наблюдаемость (хотя бы консольные логи + `/health`)     | ✅ | `GET /health` возвращает статус, количество чанков, доступность LLM. Uvicorn логирует все запросы. При старте выводится информация о загрузке данных и моделей |
| 9 | В `report.md` **обоснован выбор финальной модели** по результатам экспериментов | ✅ | `report.md` раздел 5: таблица Hit@3 (TF-IDF ~92% vs Embeddings ~85%). Несмотря на более высокое значение Hit@3 у TF-IDF на тестовом наборе запросов, embeddings выбраны как основной метод поиска, поскольку обеспечивают семантический поиск и лучше работают с перефразированными запросами. Для RAG-системы это является важным свойством. TF-IDF сохранён в проекте как baseline-модель для сравнения качества поиска. |
| 10 | `project/README.md` и `report.md` позволяют понять сценарий демонстрации | ✅ | `README.md` раздел «Как запустить». `report.md` раздел 9 «Сценарий демонстрации» с конкретными командами и примерами запросов |

---

## Подсчёт баллов и связь с оценкой

**Итого: 10/10** ✅

За каждый критерий, по которому вы честно можете поставить `✅`, засчитывается **1 балл**.  
Максимум – 10 баллов.

Преподаватель использует суммарное количество баллов **после проверки** в качестве ориентира для выставления оценки:

- Если проект **не проходит базовый минимум** (не запускается, отсутствует ключевой функционал, грубые нарушения, плагиат и т.п.) – оценка: **2** (проходной минимум не достигнут).
- Если минимум пройден, то ориентировочно:
  - **1-4 балла** → оценка **3** (проект принят, но на базовом уровне);
  - **5-8 баллов** → оценка **4** (хороший, рабочий проект);
  - **9-10 баллов** → оценка **5** (сильный, хорошо проработанный проект).

Окончательное решение по оценке остаётся за преподавателем и может учитывать:

- качество реализации внутри каждого пункта;
- дополнительные сильные стороны проекта (нестандартные решения, дополнительные функции, аккуратная архитектура и т.п.);
- соблюдение требований курса и дедлайнов.

---
